import Editor, { EditorHandler, EditorContext } from "./Editor";
import {
  getAllImages,
  replaceImages,
  removeTrailingWhitespace,
} from "./quillUtils";

export {
  getAllImages,
  replaceImages,
  removeTrailingWhitespace,
  EditorHandler,
  EditorContext,
};
export default Editor;
